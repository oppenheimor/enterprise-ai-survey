## 1. 规格落地与共享模型

- [x] 1.1 整理 16 题问卷配置、字段映射、评分常量与结果 schema，形成前后端共享模块，并用单元测试锁定关键规则计算。
- [x] 1.2 设计并落地 Prisma 数据模型，覆盖问卷答案、评分结果、LLM 结果摘要、留资信息和关键渠道字段，并准备迁移与基础读写能力。

## 2. 前台 H5 主流程

- [x] 2.1 以 Bento Grid 视觉系统重做落地页、问卷页、结果预览页、留资页和成功页，默认实现 `16 题逐题页 + 分组标题` 交互。
- [x] 2.2 接通问卷状态管理、逐题校验、结果预览与留资提交链路，确保用户能够从首页完整走到成功页。

## 3. 规则评分与 LLM 解释层

- [x] 3.1 实现服务端评分流程，产出维度分、总分、评级、主推荐场景和线索优先级，并补充固定样本回归测试。
- [x] 3.2 实现 LLM 结构化输入输出协议、失败兜底与结果页渲染，并接入 benchmark 样本校验或等价专项验证。

## 4. 最小运营看板

- [x] 4.1 实现最小运营看板页面，展示指标卡、漏斗、分布图和线索列表，并支持按评分等级、主推荐场景和线索优先级进行基础筛选。

## 5. 验证

- [x] 5.1 运行 `pnpm spec:validate`，确保 OpenSpec 产物通过校验。
- [x] 5.2 运行 `pnpm check`，并对核心用户流程补充对应 e2e、规则测试和必要的手工 smoke 记录。
